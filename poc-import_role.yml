---
- hosts: localhost
  connection: local
  gather_facts: false
  become: false

  vars: {}

  tasks:
    - include_role:
        name: use_import_role
      vars:
        final_role: false
      loop_control:
        loop_var: roleidx
      with_sequence: start=1 end=30

    - include_role:
        name: use_import_role
      vars:
        final_role: true
        roleidx: 31

  handlers: []
